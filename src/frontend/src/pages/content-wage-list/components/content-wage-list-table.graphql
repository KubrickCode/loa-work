query ContentWageListTable($filter: ContentWageListFilter) {
  contentWageList(filter: $filter) {
    content {
      contentCategory {
        imageUrl
        name
      }
      contentDuration {
        id
      }
      displayName
      durationText
      level
    }
    goldAmount
    krwAmount
  }
}

query ContentRewardItemsFilter {
  contentRewardItems {
    id
    name
  }
}

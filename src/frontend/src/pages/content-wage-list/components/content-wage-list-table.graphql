query ContentWageListTable($filter: ContentListFilter) {
  contentList(filter: $filter) {
    contentCategory {
      name
    }
    displayName
    wage {
      amount
      goldAmount
    }
  }
}

query ContentRewardItemsFilter {
  contentRewardItems
}

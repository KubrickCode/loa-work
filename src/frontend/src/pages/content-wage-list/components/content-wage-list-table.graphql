query ContentWageListTable($filter: ContentWageListFilter) {
  contentWageList(filter: $filter) {
    content {
      contentCategory {
        imageUrl
        name
      }
      displayName
      durationText
      id
      level
    }
    goldAmountPerHour
    goldAmountPerClear
    krwAmountPerHour
  }
}

query ItemsFilter {
  items {
    id
    name
  }
}

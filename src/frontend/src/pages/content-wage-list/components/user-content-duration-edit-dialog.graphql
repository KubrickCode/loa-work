query UserContentDurationEditDialog($id: Int!) {
  contentDuration(id: $id) {
    userContentDuration {
      id
      value
    }
  }
}

mutation UserContentDurationEdit($input: UserContentDurationEditInput!) {
  userContentDurationEdit(input: $input) {
    ok
  }
}

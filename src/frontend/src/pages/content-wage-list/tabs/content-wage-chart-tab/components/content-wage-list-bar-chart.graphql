query ContentWageListBarChart($filter: ContentWageListFilter) {
  contentWageList(filter: $filter) {
    content {
      contentCategory {
        name
      }
      displayName
    }
    goldAmount
    krwAmount
  }
}

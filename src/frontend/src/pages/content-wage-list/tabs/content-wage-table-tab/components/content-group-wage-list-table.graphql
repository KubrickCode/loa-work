query ContentGroupWageListTable($filter: ContentGroupWageListFilter) {
  contentGroupWageList(filter: $filter) {
    contentGroup {
      contentCategory {
        id
        imageUrl
        name
      }
      contentIds
      durationText
      level
      name
    }
    goldAmountPerHour
    goldAmountPerClear
    krwAmountPerHour
  }
}

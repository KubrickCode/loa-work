query UserExtraItemPriceEditDialog($id: Int!) {
  item(id: $id) {
    name
    userItem {
      id
      price
    }
  }
}

mutation UserItemEdit($input: UserItemEditInput!) {
  userItemEdit(input: $input) {
    ok
  }
}

query ContentRewardEditDialog($id: Int!) {
  content(id: $id) {
    contentRewards {
      averageQuantity
      id
      isSellable
      itemName
    }
  }
}

mutation ContentRewardsEdit($input: ContentRewardsEditInput!) {
  contentRewardsEdit(input: $input) {
    ok
  }
}

query ContentRewardListTable($filter: ContentListFilter) {
  contentList(filter: $filter) {
    contentCategory {
      name
    }
    contentRewards {
      averageQuantity
      contentRewardItem {
        name
      }
      isSellable
    }
    displayName
    duration
    id
    level
  }
  contentRewardItems {
    name
  }
}

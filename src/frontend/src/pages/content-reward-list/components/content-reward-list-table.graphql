query ContentRewardListTable($filter: ContentListFilter) {
  contentList(filter: $filter) {
    contentCategory {
      name
    }
    contentRewards {
      averageQuantity
      isSellable
      itemName
    }
    displayName
    duration
    id
    level
  }
  contentRewardItems
}

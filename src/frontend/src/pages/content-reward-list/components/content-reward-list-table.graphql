query ContentRewardListTable($filter: ContentListFilter) {
  contentList(filter: $filter) {
    contentCategory {
      name
    }
    contentRewards {
      averageQuantity
      isSellable
      itemName
    }
    displayName
    id
    level
  }
  contentRewardItems
}

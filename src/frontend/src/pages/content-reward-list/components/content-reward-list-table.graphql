query ContentRewardListTable($filter: ContentListFilter) {
  contentList(filter: $filter) {
    contentRewards {
      averageQuantity
      isSellable
      itemName
    }
    displayName
    displayTypeName
    level
  }
  contentRewardViewList
}

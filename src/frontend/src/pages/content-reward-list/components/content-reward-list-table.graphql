query ContentRewardListTable($filter: ContentListFilter) {
  contentList(filter: $filter) {
    contentCategory {
      name
    }
    contentRewards {
      averageQuantity
      contentRewardItem {
        name
      }
      isSellable
    }
    displayName
    durationText
    id
    level
  }
  contentRewardItems {
    name
  }
}

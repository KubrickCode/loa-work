query ContentCreateTabData {
  contentCategories {
    id
    name
  }
  contentRewardItems {
    id
    name
  }
}

mutation ContentCreateTab($input: ContentCreateInput!) {
  contentCreate(input: $input) {
    ok
  }
}

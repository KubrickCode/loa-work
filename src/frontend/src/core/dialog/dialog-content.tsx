import { DialogContentProps } from "@chakra-ui/react";

import { DialogContent as ChakraDialogContent } from "../chakra-components/ui/dialog";

export const DialogContent = (props: DialogContentProps) => (
  <ChakraDialogContent {...props} />
);

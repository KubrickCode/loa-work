query ContentDetailsDialog($contentId: Int!) {
  content(id: $contentId) {
    contentCategory {
      imageUrl
      name
    }
    contentRewards {
      averageQuantity
      contentRewardItem {
        imageUrl
        name
      }
      isSellable
    }
    displayName
    level
  }
}

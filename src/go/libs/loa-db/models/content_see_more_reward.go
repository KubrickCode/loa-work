// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/sqlboiler/v4/types"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// ContentSeeMoreReward is an object representing the database table.
type ContentSeeMoreReward struct {
	ID        int           `boil:"id" json:"id" toml:"id" yaml:"id"`
	CreatedAt time.Time     `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt time.Time     `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	Quantity  types.Decimal `boil:"quantity" json:"quantity" toml:"quantity" yaml:"quantity"`
	ContentID int           `boil:"content_id" json:"content_id" toml:"content_id" yaml:"content_id"`
	ItemID    int           `boil:"item_id" json:"item_id" toml:"item_id" yaml:"item_id"`

	R *contentSeeMoreRewardR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L contentSeeMoreRewardL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ContentSeeMoreRewardColumns = struct {
	ID        string
	CreatedAt string
	UpdatedAt string
	Quantity  string
	ContentID string
	ItemID    string
}{
	ID:        "id",
	CreatedAt: "created_at",
	UpdatedAt: "updated_at",
	Quantity:  "quantity",
	ContentID: "content_id",
	ItemID:    "item_id",
}

var ContentSeeMoreRewardTableColumns = struct {
	ID        string
	CreatedAt string
	UpdatedAt string
	Quantity  string
	ContentID string
	ItemID    string
}{
	ID:        "content_see_more_reward.id",
	CreatedAt: "content_see_more_reward.created_at",
	UpdatedAt: "content_see_more_reward.updated_at",
	Quantity:  "content_see_more_reward.quantity",
	ContentID: "content_see_more_reward.content_id",
	ItemID:    "content_see_more_reward.item_id",
}

// Generated where

var ContentSeeMoreRewardWhere = struct {
	ID        whereHelperint
	CreatedAt whereHelpertime_Time
	UpdatedAt whereHelpertime_Time
	Quantity  whereHelpertypes_Decimal
	ContentID whereHelperint
	ItemID    whereHelperint
}{
	ID:        whereHelperint{field: "\"content_see_more_reward\".\"id\""},
	CreatedAt: whereHelpertime_Time{field: "\"content_see_more_reward\".\"created_at\""},
	UpdatedAt: whereHelpertime_Time{field: "\"content_see_more_reward\".\"updated_at\""},
	Quantity:  whereHelpertypes_Decimal{field: "\"content_see_more_reward\".\"quantity\""},
	ContentID: whereHelperint{field: "\"content_see_more_reward\".\"content_id\""},
	ItemID:    whereHelperint{field: "\"content_see_more_reward\".\"item_id\""},
}

// ContentSeeMoreRewardRels is where relationship names are stored.
var ContentSeeMoreRewardRels = struct {
	Content string
	Item    string
}{
	Content: "Content",
	Item:    "Item",
}

// contentSeeMoreRewardR is where relationships are stored.
type contentSeeMoreRewardR struct {
	Content *Content `boil:"Content" json:"Content" toml:"Content" yaml:"Content"`
	Item    *Item    `boil:"Item" json:"Item" toml:"Item" yaml:"Item"`
}

// NewStruct creates a new relationship struct
func (*contentSeeMoreRewardR) NewStruct() *contentSeeMoreRewardR {
	return &contentSeeMoreRewardR{}
}

func (o *ContentSeeMoreReward) GetContent() *Content {
	if o == nil {
		return nil
	}

	return o.R.GetContent()
}

func (r *contentSeeMoreRewardR) GetContent() *Content {
	if r == nil {
		return nil
	}

	return r.Content
}

func (o *ContentSeeMoreReward) GetItem() *Item {
	if o == nil {
		return nil
	}

	return o.R.GetItem()
}

func (r *contentSeeMoreRewardR) GetItem() *Item {
	if r == nil {
		return nil
	}

	return r.Item
}

// contentSeeMoreRewardL is where Load methods for each relationship are stored.
type contentSeeMoreRewardL struct{}

var (
	contentSeeMoreRewardAllColumns            = []string{"id", "created_at", "updated_at", "quantity", "content_id", "item_id"}
	contentSeeMoreRewardColumnsWithoutDefault = []string{"updated_at", "content_id", "item_id"}
	contentSeeMoreRewardColumnsWithDefault    = []string{"id", "created_at", "quantity"}
	contentSeeMoreRewardPrimaryKeyColumns     = []string{"id"}
	contentSeeMoreRewardGeneratedColumns      = []string{}
)

type (
	// ContentSeeMoreRewardSlice is an alias for a slice of pointers to ContentSeeMoreReward.
	// This should almost always be used instead of []ContentSeeMoreReward.
	ContentSeeMoreRewardSlice []*ContentSeeMoreReward
	// ContentSeeMoreRewardHook is the signature for custom ContentSeeMoreReward hook methods
	ContentSeeMoreRewardHook func(context.Context, boil.ContextExecutor, *ContentSeeMoreReward) error

	contentSeeMoreRewardQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	contentSeeMoreRewardType                 = reflect.TypeOf(&ContentSeeMoreReward{})
	contentSeeMoreRewardMapping              = queries.MakeStructMapping(contentSeeMoreRewardType)
	contentSeeMoreRewardPrimaryKeyMapping, _ = queries.BindMapping(contentSeeMoreRewardType, contentSeeMoreRewardMapping, contentSeeMoreRewardPrimaryKeyColumns)
	contentSeeMoreRewardInsertCacheMut       sync.RWMutex
	contentSeeMoreRewardInsertCache          = make(map[string]insertCache)
	contentSeeMoreRewardUpdateCacheMut       sync.RWMutex
	contentSeeMoreRewardUpdateCache          = make(map[string]updateCache)
	contentSeeMoreRewardUpsertCacheMut       sync.RWMutex
	contentSeeMoreRewardUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var contentSeeMoreRewardAfterSelectMu sync.Mutex
var contentSeeMoreRewardAfterSelectHooks []ContentSeeMoreRewardHook

var contentSeeMoreRewardBeforeInsertMu sync.Mutex
var contentSeeMoreRewardBeforeInsertHooks []ContentSeeMoreRewardHook
var contentSeeMoreRewardAfterInsertMu sync.Mutex
var contentSeeMoreRewardAfterInsertHooks []ContentSeeMoreRewardHook

var contentSeeMoreRewardBeforeUpdateMu sync.Mutex
var contentSeeMoreRewardBeforeUpdateHooks []ContentSeeMoreRewardHook
var contentSeeMoreRewardAfterUpdateMu sync.Mutex
var contentSeeMoreRewardAfterUpdateHooks []ContentSeeMoreRewardHook

var contentSeeMoreRewardBeforeDeleteMu sync.Mutex
var contentSeeMoreRewardBeforeDeleteHooks []ContentSeeMoreRewardHook
var contentSeeMoreRewardAfterDeleteMu sync.Mutex
var contentSeeMoreRewardAfterDeleteHooks []ContentSeeMoreRewardHook

var contentSeeMoreRewardBeforeUpsertMu sync.Mutex
var contentSeeMoreRewardBeforeUpsertHooks []ContentSeeMoreRewardHook
var contentSeeMoreRewardAfterUpsertMu sync.Mutex
var contentSeeMoreRewardAfterUpsertHooks []ContentSeeMoreRewardHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *ContentSeeMoreReward) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range contentSeeMoreRewardAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *ContentSeeMoreReward) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range contentSeeMoreRewardBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *ContentSeeMoreReward) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range contentSeeMoreRewardAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *ContentSeeMoreReward) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range contentSeeMoreRewardBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *ContentSeeMoreReward) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range contentSeeMoreRewardAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *ContentSeeMoreReward) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range contentSeeMoreRewardBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *ContentSeeMoreReward) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range contentSeeMoreRewardAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *ContentSeeMoreReward) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range contentSeeMoreRewardBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *ContentSeeMoreReward) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range contentSeeMoreRewardAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddContentSeeMoreRewardHook registers your hook function for all future operations.
func AddContentSeeMoreRewardHook(hookPoint boil.HookPoint, contentSeeMoreRewardHook ContentSeeMoreRewardHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		contentSeeMoreRewardAfterSelectMu.Lock()
		contentSeeMoreRewardAfterSelectHooks = append(contentSeeMoreRewardAfterSelectHooks, contentSeeMoreRewardHook)
		contentSeeMoreRewardAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		contentSeeMoreRewardBeforeInsertMu.Lock()
		contentSeeMoreRewardBeforeInsertHooks = append(contentSeeMoreRewardBeforeInsertHooks, contentSeeMoreRewardHook)
		contentSeeMoreRewardBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		contentSeeMoreRewardAfterInsertMu.Lock()
		contentSeeMoreRewardAfterInsertHooks = append(contentSeeMoreRewardAfterInsertHooks, contentSeeMoreRewardHook)
		contentSeeMoreRewardAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		contentSeeMoreRewardBeforeUpdateMu.Lock()
		contentSeeMoreRewardBeforeUpdateHooks = append(contentSeeMoreRewardBeforeUpdateHooks, contentSeeMoreRewardHook)
		contentSeeMoreRewardBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		contentSeeMoreRewardAfterUpdateMu.Lock()
		contentSeeMoreRewardAfterUpdateHooks = append(contentSeeMoreRewardAfterUpdateHooks, contentSeeMoreRewardHook)
		contentSeeMoreRewardAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		contentSeeMoreRewardBeforeDeleteMu.Lock()
		contentSeeMoreRewardBeforeDeleteHooks = append(contentSeeMoreRewardBeforeDeleteHooks, contentSeeMoreRewardHook)
		contentSeeMoreRewardBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		contentSeeMoreRewardAfterDeleteMu.Lock()
		contentSeeMoreRewardAfterDeleteHooks = append(contentSeeMoreRewardAfterDeleteHooks, contentSeeMoreRewardHook)
		contentSeeMoreRewardAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		contentSeeMoreRewardBeforeUpsertMu.Lock()
		contentSeeMoreRewardBeforeUpsertHooks = append(contentSeeMoreRewardBeforeUpsertHooks, contentSeeMoreRewardHook)
		contentSeeMoreRewardBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		contentSeeMoreRewardAfterUpsertMu.Lock()
		contentSeeMoreRewardAfterUpsertHooks = append(contentSeeMoreRewardAfterUpsertHooks, contentSeeMoreRewardHook)
		contentSeeMoreRewardAfterUpsertMu.Unlock()
	}
}

// OneG returns a single contentSeeMoreReward record from the query using the global executor.
func (q contentSeeMoreRewardQuery) OneG(ctx context.Context) (*ContentSeeMoreReward, error) {
	return q.One(ctx, boil.GetContextDB())
}

// One returns a single contentSeeMoreReward record from the query.
func (q contentSeeMoreRewardQuery) One(ctx context.Context, exec boil.ContextExecutor) (*ContentSeeMoreReward, error) {
	o := &ContentSeeMoreReward{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for content_see_more_reward")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all ContentSeeMoreReward records from the query using the global executor.
func (q contentSeeMoreRewardQuery) AllG(ctx context.Context) (ContentSeeMoreRewardSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// All returns all ContentSeeMoreReward records from the query.
func (q contentSeeMoreRewardQuery) All(ctx context.Context, exec boil.ContextExecutor) (ContentSeeMoreRewardSlice, error) {
	var o []*ContentSeeMoreReward

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to ContentSeeMoreReward slice")
	}

	if len(contentSeeMoreRewardAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all ContentSeeMoreReward records in the query using the global executor
func (q contentSeeMoreRewardQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// Count returns the count of all ContentSeeMoreReward records in the query.
func (q contentSeeMoreRewardQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count content_see_more_reward rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q contentSeeMoreRewardQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// Exists checks if the row exists in the table.
func (q contentSeeMoreRewardQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if content_see_more_reward exists")
	}

	return count > 0, nil
}

// Content pointed to by the foreign key.
func (o *ContentSeeMoreReward) Content(mods ...qm.QueryMod) contentQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ContentID),
	}

	queryMods = append(queryMods, mods...)

	return Contents(queryMods...)
}

// Item pointed to by the foreign key.
func (o *ContentSeeMoreReward) Item(mods ...qm.QueryMod) itemQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ItemID),
	}

	queryMods = append(queryMods, mods...)

	return Items(queryMods...)
}

// LoadContent allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (contentSeeMoreRewardL) LoadContent(ctx context.Context, e boil.ContextExecutor, singular bool, maybeContentSeeMoreReward interface{}, mods queries.Applicator) error {
	var slice []*ContentSeeMoreReward
	var object *ContentSeeMoreReward

	if singular {
		var ok bool
		object, ok = maybeContentSeeMoreReward.(*ContentSeeMoreReward)
		if !ok {
			object = new(ContentSeeMoreReward)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeContentSeeMoreReward)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeContentSeeMoreReward))
			}
		}
	} else {
		s, ok := maybeContentSeeMoreReward.(*[]*ContentSeeMoreReward)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeContentSeeMoreReward)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeContentSeeMoreReward))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &contentSeeMoreRewardR{}
		}
		args[object.ContentID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &contentSeeMoreRewardR{}
			}

			args[obj.ContentID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`content`),
		qm.WhereIn(`content.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Content")
	}

	var resultSlice []*Content
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Content")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for content")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for content")
	}

	if len(contentAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Content = foreign
		if foreign.R == nil {
			foreign.R = &contentR{}
		}
		foreign.R.ContentSeeMoreRewards = append(foreign.R.ContentSeeMoreRewards, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ContentID == foreign.ID {
				local.R.Content = foreign
				if foreign.R == nil {
					foreign.R = &contentR{}
				}
				foreign.R.ContentSeeMoreRewards = append(foreign.R.ContentSeeMoreRewards, local)
				break
			}
		}
	}

	return nil
}

// LoadItem allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (contentSeeMoreRewardL) LoadItem(ctx context.Context, e boil.ContextExecutor, singular bool, maybeContentSeeMoreReward interface{}, mods queries.Applicator) error {
	var slice []*ContentSeeMoreReward
	var object *ContentSeeMoreReward

	if singular {
		var ok bool
		object, ok = maybeContentSeeMoreReward.(*ContentSeeMoreReward)
		if !ok {
			object = new(ContentSeeMoreReward)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeContentSeeMoreReward)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeContentSeeMoreReward))
			}
		}
	} else {
		s, ok := maybeContentSeeMoreReward.(*[]*ContentSeeMoreReward)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeContentSeeMoreReward)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeContentSeeMoreReward))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &contentSeeMoreRewardR{}
		}
		args[object.ItemID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &contentSeeMoreRewardR{}
			}

			args[obj.ItemID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`item`),
		qm.WhereIn(`item.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Item")
	}

	var resultSlice []*Item
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Item")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for item")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for item")
	}

	if len(itemAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Item = foreign
		if foreign.R == nil {
			foreign.R = &itemR{}
		}
		foreign.R.ContentSeeMoreRewards = append(foreign.R.ContentSeeMoreRewards, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ItemID == foreign.ID {
				local.R.Item = foreign
				if foreign.R == nil {
					foreign.R = &itemR{}
				}
				foreign.R.ContentSeeMoreRewards = append(foreign.R.ContentSeeMoreRewards, local)
				break
			}
		}
	}

	return nil
}

// SetContentG of the contentSeeMoreReward to the related item.
// Sets o.R.Content to related.
// Adds o to related.R.ContentSeeMoreRewards.
// Uses the global database handle.
func (o *ContentSeeMoreReward) SetContentG(ctx context.Context, insert bool, related *Content) error {
	return o.SetContent(ctx, boil.GetContextDB(), insert, related)
}

// SetContent of the contentSeeMoreReward to the related item.
// Sets o.R.Content to related.
// Adds o to related.R.ContentSeeMoreRewards.
func (o *ContentSeeMoreReward) SetContent(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Content) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"content_see_more_reward\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"content_id"}),
		strmangle.WhereClause("\"", "\"", 2, contentSeeMoreRewardPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ContentID = related.ID
	if o.R == nil {
		o.R = &contentSeeMoreRewardR{
			Content: related,
		}
	} else {
		o.R.Content = related
	}

	if related.R == nil {
		related.R = &contentR{
			ContentSeeMoreRewards: ContentSeeMoreRewardSlice{o},
		}
	} else {
		related.R.ContentSeeMoreRewards = append(related.R.ContentSeeMoreRewards, o)
	}

	return nil
}

// SetItemG of the contentSeeMoreReward to the related item.
// Sets o.R.Item to related.
// Adds o to related.R.ContentSeeMoreRewards.
// Uses the global database handle.
func (o *ContentSeeMoreReward) SetItemG(ctx context.Context, insert bool, related *Item) error {
	return o.SetItem(ctx, boil.GetContextDB(), insert, related)
}

// SetItem of the contentSeeMoreReward to the related item.
// Sets o.R.Item to related.
// Adds o to related.R.ContentSeeMoreRewards.
func (o *ContentSeeMoreReward) SetItem(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Item) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"content_see_more_reward\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"item_id"}),
		strmangle.WhereClause("\"", "\"", 2, contentSeeMoreRewardPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ItemID = related.ID
	if o.R == nil {
		o.R = &contentSeeMoreRewardR{
			Item: related,
		}
	} else {
		o.R.Item = related
	}

	if related.R == nil {
		related.R = &itemR{
			ContentSeeMoreRewards: ContentSeeMoreRewardSlice{o},
		}
	} else {
		related.R.ContentSeeMoreRewards = append(related.R.ContentSeeMoreRewards, o)
	}

	return nil
}

// ContentSeeMoreRewards retrieves all the records using an executor.
func ContentSeeMoreRewards(mods ...qm.QueryMod) contentSeeMoreRewardQuery {
	mods = append(mods, qm.From("\"content_see_more_reward\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"content_see_more_reward\".*"})
	}

	return contentSeeMoreRewardQuery{q}
}

// FindContentSeeMoreRewardG retrieves a single record by ID.
func FindContentSeeMoreRewardG(ctx context.Context, iD int, selectCols ...string) (*ContentSeeMoreReward, error) {
	return FindContentSeeMoreReward(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindContentSeeMoreReward retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindContentSeeMoreReward(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*ContentSeeMoreReward, error) {
	contentSeeMoreRewardObj := &ContentSeeMoreReward{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"content_see_more_reward\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, contentSeeMoreRewardObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from content_see_more_reward")
	}

	if err = contentSeeMoreRewardObj.doAfterSelectHooks(ctx, exec); err != nil {
		return contentSeeMoreRewardObj, err
	}

	return contentSeeMoreRewardObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *ContentSeeMoreReward) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ContentSeeMoreReward) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no content_see_more_reward provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(contentSeeMoreRewardColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	contentSeeMoreRewardInsertCacheMut.RLock()
	cache, cached := contentSeeMoreRewardInsertCache[key]
	contentSeeMoreRewardInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			contentSeeMoreRewardAllColumns,
			contentSeeMoreRewardColumnsWithDefault,
			contentSeeMoreRewardColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(contentSeeMoreRewardType, contentSeeMoreRewardMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(contentSeeMoreRewardType, contentSeeMoreRewardMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"content_see_more_reward\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"content_see_more_reward\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into content_see_more_reward")
	}

	if !cached {
		contentSeeMoreRewardInsertCacheMut.Lock()
		contentSeeMoreRewardInsertCache[key] = cache
		contentSeeMoreRewardInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single ContentSeeMoreReward record using the global executor.
// See Update for more documentation.
func (o *ContentSeeMoreReward) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// Update uses an executor to update the ContentSeeMoreReward.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ContentSeeMoreReward) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	contentSeeMoreRewardUpdateCacheMut.RLock()
	cache, cached := contentSeeMoreRewardUpdateCache[key]
	contentSeeMoreRewardUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			contentSeeMoreRewardAllColumns,
			contentSeeMoreRewardPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update content_see_more_reward, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"content_see_more_reward\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, contentSeeMoreRewardPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(contentSeeMoreRewardType, contentSeeMoreRewardMapping, append(wl, contentSeeMoreRewardPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update content_see_more_reward row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for content_see_more_reward")
	}

	if !cached {
		contentSeeMoreRewardUpdateCacheMut.Lock()
		contentSeeMoreRewardUpdateCache[key] = cache
		contentSeeMoreRewardUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllG updates all rows with the specified column values.
func (q contentSeeMoreRewardQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values.
func (q contentSeeMoreRewardQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for content_see_more_reward")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for content_see_more_reward")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o ContentSeeMoreRewardSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ContentSeeMoreRewardSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), contentSeeMoreRewardPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"content_see_more_reward\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, contentSeeMoreRewardPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in contentSeeMoreReward slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all contentSeeMoreReward")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *ContentSeeMoreReward) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ContentSeeMoreReward) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("models: no content_see_more_reward provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(contentSeeMoreRewardColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	contentSeeMoreRewardUpsertCacheMut.RLock()
	cache, cached := contentSeeMoreRewardUpsertCache[key]
	contentSeeMoreRewardUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			contentSeeMoreRewardAllColumns,
			contentSeeMoreRewardColumnsWithDefault,
			contentSeeMoreRewardColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			contentSeeMoreRewardAllColumns,
			contentSeeMoreRewardPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert content_see_more_reward, could not build update column list")
		}

		ret := strmangle.SetComplement(contentSeeMoreRewardAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(contentSeeMoreRewardPrimaryKeyColumns) == 0 {
				return errors.New("models: unable to upsert content_see_more_reward, could not build conflict column list")
			}

			conflict = make([]string, len(contentSeeMoreRewardPrimaryKeyColumns))
			copy(conflict, contentSeeMoreRewardPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"content_see_more_reward\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(contentSeeMoreRewardType, contentSeeMoreRewardMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(contentSeeMoreRewardType, contentSeeMoreRewardMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert content_see_more_reward")
	}

	if !cached {
		contentSeeMoreRewardUpsertCacheMut.Lock()
		contentSeeMoreRewardUpsertCache[key] = cache
		contentSeeMoreRewardUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single ContentSeeMoreReward record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *ContentSeeMoreReward) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// Delete deletes a single ContentSeeMoreReward record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ContentSeeMoreReward) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no ContentSeeMoreReward provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), contentSeeMoreRewardPrimaryKeyMapping)
	sql := "DELETE FROM \"content_see_more_reward\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from content_see_more_reward")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for content_see_more_reward")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q contentSeeMoreRewardQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all matching rows.
func (q contentSeeMoreRewardQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no contentSeeMoreRewardQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from content_see_more_reward")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for content_see_more_reward")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o ContentSeeMoreRewardSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ContentSeeMoreRewardSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(contentSeeMoreRewardBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), contentSeeMoreRewardPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"content_see_more_reward\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, contentSeeMoreRewardPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from contentSeeMoreReward slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for content_see_more_reward")
	}

	if len(contentSeeMoreRewardAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *ContentSeeMoreReward) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: no ContentSeeMoreReward provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ContentSeeMoreReward) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindContentSeeMoreReward(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ContentSeeMoreRewardSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: empty ContentSeeMoreRewardSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ContentSeeMoreRewardSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ContentSeeMoreRewardSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), contentSeeMoreRewardPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"content_see_more_reward\".* FROM \"content_see_more_reward\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, contentSeeMoreRewardPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in ContentSeeMoreRewardSlice")
	}

	*o = slice

	return nil
}

// ContentSeeMoreRewardExistsG checks if the ContentSeeMoreReward row exists.
func ContentSeeMoreRewardExistsG(ctx context.Context, iD int) (bool, error) {
	return ContentSeeMoreRewardExists(ctx, boil.GetContextDB(), iD)
}

// ContentSeeMoreRewardExists checks if the ContentSeeMoreReward row exists.
func ContentSeeMoreRewardExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"content_see_more_reward\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if content_see_more_reward exists")
	}

	return exists, nil
}

// Exists checks if the ContentSeeMoreReward row exists.
func (o *ContentSeeMoreReward) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return ContentSeeMoreRewardExists(ctx, exec, o.ID)
}
